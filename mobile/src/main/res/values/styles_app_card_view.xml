<resources>

    <style name="AppTheme.CardView.Default" parent="android:style/Widget">
        <item name="cardCornerRadius">@dimen/_8sdp</item>
        <item name="cardElevation">@dimen/_2sdp</item>
        <item name="cardBackgroundColor">@color/white</item>
    </style>

</resources>
